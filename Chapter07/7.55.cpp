// Exercise 7.55:
/*
 Is the Data class from § 7.5.5 (p. 298) a literal class?
 If not, why not?
 If so, explain why it is literal.
 
 The code below is not a literal class because it doesn't return literal values (they are not initialised)
 
*/

struct Data {
    int ival;
    string s;
};
