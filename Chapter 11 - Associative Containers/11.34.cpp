// Exercise 11.34:
/*
 What would happen if we used the subscript operator instead of find in the transform function?
 
 >> If we were to use to subscript operator, then should the key not exist already, then it will be created and inserted into the map with an empty associative value
*/
