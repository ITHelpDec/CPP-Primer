// Exercise 15.33:
/*
 What about objects of type Query_base?
 
 >> This will be managed by the default synthsised copy-control.
 >> N.B.: Query_base is set up as an "abstract base class", so we really only intend to derive other classes from this interface, rather than apply constructors or copy-control to it.
*/
