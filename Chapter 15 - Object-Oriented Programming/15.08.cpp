// Exercise 15.08:
/*
 Define static type and dynamic type.
 
 >> .:. STATIC TYPE .:.
 // "The static type of an expression is always known at compile time – it is the type with which a variable is declared or that an expression yields." - pg. 601
 
 >> .:. DYNAMIC TYPE .:.
 "The dynamic type is the type of the object in memory that the variable or expression represents. The dynamic type may not be known until run time." – pg. 601
*/
